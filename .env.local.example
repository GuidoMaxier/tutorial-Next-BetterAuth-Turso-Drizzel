# --- TURSO DATABASE ---
# Consigue la URL ejecutando: turso db show <nombre-db> --url
TURSO_CONNECTION_URL=libsql://tu-db-url.turso.io

# Consigue el Token ejecutando: turso db tokens create <nombre-db>
TURSO_AUTH_TOKEN=tu-auth-token-aqui

# --- BETTER AUTH CONFIG ---
# Genera un secreto aleatorio con: openssl rand -base64 32
BETTER_AUTH_SECRET=genera-un-secreto-muy-largo-y-seguro

# En desarrollo usa http://localhost:3000
BETTER_AUTH_URL=http://localhost:3000

# --- GOOGLE PROVIDER ---
# Consigue estos en https://console.cloud.google.com/
# Recuerda añadir la URI de redirección: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=tu-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu-google-client-secret

# --- PUBLIC VARIABLES ---
# Requerido para que el cliente de Better Auth funcione en el navegador
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
